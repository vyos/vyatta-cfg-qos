type: u32
help: Minimum threshold for random detection
syntax:expression: $VAR(@) >= 0 && $VAR(@) <= 4096; \
                   "Threshold must be between 0 and 4096"
val_help: u32:0-4096; Minimum Threshold in packets
