type: u32
help: Set ordering for rule evaluation
default: 20
syntax:expression: $VAR(@) >= 0 && $VAR(@) <= 255 ; \
		   "precedence must be between 0 and 255"
comp_help: Precedence in traffic limiter rule evaluation
 <0-255> (default 20)
