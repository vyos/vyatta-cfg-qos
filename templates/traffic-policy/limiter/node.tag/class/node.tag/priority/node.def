type: u32
help: Priority for rule evaluation
default: 20
syntax:expression: $VAR(@) >= 0 && $VAR(@) <= 20 ; \
		   "priority must be between 0 and 20"
help: Priority value
val_help: u32:0-20; Priority for match rule evaluation
